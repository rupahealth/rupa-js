name: CI
on: push
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install
      run: npm install
    - name: Test
      run: npm run test
    - name: Lint
      run: npm run lint
